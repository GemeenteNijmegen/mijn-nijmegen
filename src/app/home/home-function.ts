// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for HomeFunction
 */
export interface HomeFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/home/home.
 */
export class HomeFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: HomeFunctionProps) {
    super(scope, id, {
      description: 'src/app/home/home.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs18.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/home/home.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}