// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for ProductenFunction
 */
export interface ProductenFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/producten/producten.
 */
export class ProductenFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: ProductenFunctionProps) {
    super(scope, id, {
      description: 'src/app/producten/producten.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/producten/producten.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}