// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for TakenFunction
 */
export interface TakenFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/taken/taken.
 */
export class TakenFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: TakenFunctionProps) {
    super(scope, id, {
      description: 'src/app/taken/taken.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/taken/taken.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}