{{>header}}

<main id="denhaag-theme nijmegen-theme">
    <!-- START: template component(s) -->
    <div class="container denhaag-theme">
        
        <div class="row">
        
        <div class="col-lg-9 col-md-12">
            {{#timeout}}
            <div id="zaak">
            <h1>Zaak</h1>
            <p>Bezig met ophalen van zaakinformatie… {{>spinner}}</p>
            </div>
            {{/timeout}}
            {{^timeout}}
            {{{singlezaak}}}
            {{/timeout}}
            </div>
        </div>
    </div>
    <!-- END: template component(s) -->
</main>
{{>footer}}
