// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for PersoonsgegevensFunction
 */
export interface PersoonsgegevensFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/app/persoonsgegevens/persoonsgegevens.
 */
export class PersoonsgegevensFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: PersoonsgegevensFunctionProps) {
    super(scope, id, {
      description: 'src/app/persoonsgegevens/persoonsgegevens.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs20.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/app/persoonsgegevens/persoonsgegevens.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}